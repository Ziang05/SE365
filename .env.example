# Biến môi trường cho Frontend
# Copy file này thành .env.local rồi điền giá trị thực

# URL backend FastAPI (để trống nếu dùng Vite proxy local dev)
# Ví dụ production: VITE_API_URL=https://your-backend.railway.app
VITE_API_URL=
